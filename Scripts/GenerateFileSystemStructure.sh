#InfoBrw: This script generates the backbone of the file system and assigns a code for each file which can be used latefor following tasks:
# Providing information about the file
# Linking this code with other files as per their relationships.
# Assigning a type like binary,configuration, directory(is also a file), script, image etc
# Hashvalue: can be used in future for security applications
